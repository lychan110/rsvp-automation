# UI Prototype

This folder is reserved for future user interface work.

The current repository implements a reusable invite and RSVP workflow through Python and Google Apps Script automation. The UI prototype is not yet implemented.
