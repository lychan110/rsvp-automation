version: '3'
services:
  inviteflow:
    build: .
    container_name: rsvp-inviteflow
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    restart: unless-stopped
