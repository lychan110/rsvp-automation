# Copy this file to .env and set your values.
# .env is NOT committed to git.
# The VITE_APP_VERSION variable is the single source of truth for the app version.
# Update it here and it propagates to HTML titles, docs, and GAS code comments on build.
VITE_APP_VERSION=4.1.0
